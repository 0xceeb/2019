id: phpmyadmin-panel

info:
  name: phpMyAdmin Panel
  author: bauthard
  severity: info

requests:
  - method: GET
    path:
      - "{{BaseURL}}/phpmyadmin/"

    matchers:
      - type: word
        words:
          - "<title>phpMyAdmin</title>"
